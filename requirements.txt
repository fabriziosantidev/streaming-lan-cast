# Streaming LAN Cast helper: Python runtime dependencies.
# Canonical list. Install with:  pip install -r requirements.txt
# The Linux/macOS installer and the Windows PyInstaller build install this same set.
streamlink       # resolve and pull the live stream from the page
pychromecast     # Google Cast (Chromecast / Android TV) targets; pulls in zeroconf for mDNS discovery
